{
  "name": "@enterprise-integration/docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "nx": "nx",
    "lint": "yarn nx lint",
    "build": "yarn nx build",
    "test": "yarn nx test",
    "test:unit": "yarn run test",
    "e2e": "yarn nx e2e",
    "test:component": "yarn run e2e",
    "format": "yarn nx format:write",
    "format:write": "yarn nx format:write",
    "format:check": "yarn nx format:check",
    "dep-graph": "yarn nx dep-graph",
    "serve": "yarn nx serve",
    "serve:dev": "yarn nx run-many --all --target=serve",
    "affected": "yarn nx affected",
    "affected:apps": "yarn nx affected:apps",
    "affected:libs": "yarn nx affected:libs",
    "affected:lint": "yarn nx affected:lint",
    "affected:build": "yarn nx affected:build",
    "affected:test:unit": "yarn nx affected:test",
    "affected:test:component": "yarn nx affected:e2e",
    "affected:test:integration": "yarn nx affected --target=e2e-integration",
    "affected:version": "yarn version decline",
    "affected:deploy": "yarn nx affected --target=deploy",
    "affected:destroy": "yarn nx affected --target=destroy",
    "workspaces:lint": "yarn nx workspace-lint"
  },
  "devDependencies": {
    "@enterprise-integration/services": "workspace:*",
    "@nrwl/cli": "13.10.1",
    "@nrwl/cypress": "13.10.1",
    "@nrwl/eslint-plugin-nx": "13.10.1",
    "@nrwl/jest": "13.10.1",
    "@nrwl/linter": "13.10.1",
    "@nrwl/tao": "13.10.1",
    "@nrwl/web": "13.10.1",
    "@nrwl/workspace": "13.10.1",
    "@types/eslint": "^8",
    "@typescript-eslint/eslint-plugin": "~5.20.0",
    "@typescript-eslint/parser": "~5.18.0",
    "eslint": "^8.0.0",
    "nx": "13.10.1"
  }
}
